<h1>List of Authors</h1>
<ul>
    <% if (authors.length > 0) { %>
        <% authors.forEach(author => { %>
            <li><%= author.name %></li>
        <% }); %>
    <% } else { %>
        <li>No authors has been found</li>
    <% } %>
</ul>